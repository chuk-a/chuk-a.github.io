name: Weather Scraper

on:
  schedule:
    - cron: '*/15 * * * *'  # every 15 minutes
  workflow_dispatch:        # manual trigger
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: write  # needed for auto-commit

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python 3.10
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install chromedriver-autoinstaller selenium pytz
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    - name: Install Chrome
      run: |
        sudo apt update
        sudo apt install -y wget unzip xvfb libxi6
        wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
        sudo apt install -y ./google-chrome-stable_current_amd64.deb

    - name: Run weather scraper
      run: python weather/weather_scraper.py

    - name: Debug workspace and file changes
      run: |
        echo "=== GITHUB_WORKSPACE ==="
        echo "$GITHUB_WORKSPACE"
        echo "=== PWD ==="
        pwd
        echo "=== Directory listing ==="
        ls -al
        echo "=== Weather directory listing ==="
        ls -al weather || echo "No weather directory"
        echo "=== Find weather_log.csv ==="
        find . -name weather_log.csv
        echo "=== Git status ==="
        git status
        echo "=== Git diff ==="
        git diff weather/weather_log.csv || echo "No diff"
        echo "=== End debug ==="

    - name: Auto-commit updated CSV
      run: |
        cd $GITHUB_WORKSPACE
        # If your script writes to weather_log.csv in root, use this:
        git add weather_log.csv
        # If your script writes to weather/weather_log.csv, use this:
        # git add weather/weather_log.csv
        git config --global user.name "github-actions"
        git config --global user.email "github-actions@github.com"
        git commit -m "Update weather log [auto]" || echo "No changes to commit"
        git push
